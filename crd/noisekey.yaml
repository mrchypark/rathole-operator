openAPIV3Schema:
  type: object
  properties:
    spec:
      type: object
      properties:
        secretName:
          type: string
        publicKeyField:
          type: string
          default: "public"
        privateKeyField:
          type: string
          default: "private"
